.venv/
__pycache__/
.DS_Store
downloads/

# Disposable weather chunks - re-derivable from the archive API, and there are
# 60 of them. data/weather_daily.csv is the consolidated output we do track.
data/_cache/

# 3.2 MB raw upstream download, re-fetched on every run. Only demand_daily.csv
# is derived from it, and that is tracked.
data/POSOCO_data.csv
